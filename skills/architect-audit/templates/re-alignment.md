# RE-ALIGNMENT — <project name>

> Generated by architect-audit on <date>. This file is a map for a fresh agent session.
> **Start here** → read "Agreed intent" and the "Verified no-regress list" before anything else, then pick up from "Ranked next steps." Do not re-litigate settled decisions.

## Agreed intent

What this project is *supposed* to be, reconstructed from artifacts and confirmed with the project owner. Each item carries its source and confidence grade.

- <claim> — source: <file/commit>, confidence: artifact | inferred | memory

## Verified no-regress list

Constraints that must NOT be undone. Treat these as load-bearing:

- <constraint>

## Deltas and verdicts

Each divergence between the current project and the agreed intent, with the owner's verdict.

| # | Type | What changed | Evidence | Verdict | Disposition |
|---|------|-------------|----------|---------|-------------|
| 1 | idea / business-logic / structure | <current vs intended> | <file:line, commit, before → after> | deliberate / accidental | re-align to intent / re-document as new intent |

## Decisions

What the audit settled, so a fresh session doesn't re-open it.

- <decision>

## Ranked next steps

Actions for a fresh session, most important first. Each step names the file/area it touches.

1. <action>

## Open questions

Anything unresolved — the fresh session should raise these early rather than assume.

- <question>
